#include <stdio.h>

int main(void)
{
    int one, two, three, four, five;

    int c;
    
    int charcount[10];

    for(int i = 0; i<10; i++) {
        charcount[i] = 0;
        printf("%d",charcount[i]);
    }

    while((c = getchar()) != EOF) {
             
    }
}
